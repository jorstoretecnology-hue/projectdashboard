{
  "mcpServers": {
    "n8n": {
      "command": "npx.cmd",
      "args": [
        "@drballs/n8n-mcp-server"
      ],
      "env": {
        "N8N_HOST": "http://localhost:5678",
        "N8N_API_KEY": "${N8N_API_KEY}"
      }
    }
  }
}
